const CASPER_DATA=(()=>{let cache=null;const files={sectors:'data/sectors.json',clubs:'data/clubs.json',players:'data/player-registry.json',competitions:'data/competitions.json',dashboard:'data/dashboard.json'};const arr=(o,k)=>Array.isArray(o?.[k])?o[k]:[];const unique=(items,label,errors,strictThree=false)=>{const seen=new Set;for(const x of items){const id=x?.nickname;if(!id)errors.push(`${label}: missing General ID`);else if(strictThree&&!/^[a-z]{3}$/i.test(id))errors.push(`${label}: invalid General ID ${id}`);else if(seen.has(id.toLowerCase()))errors.push(`${label}: duplicate General ID ${id}`);else seen.add(id.toLowerCase())}};async function fetchJSON(path){const r=await fetch(`${path}?v=${Date.now()}`,{cache:'no-store'});if(!r.ok)throw new Error(`${path} returned HTTP ${r.status}`);return r.json()}function validate(d){const errors=[],warnings=[];for(const k of Object.keys(files))if(!d[k])errors.push(`Missing dataset: ${k}`);const sectors=arr(d.sectors,'sectors'),clubs=arr(d.clubs,'clubs'),players=arr(d.players,'players'),competitions=arr(d.competitions,'competitions');unique(sectors,'sector',errors);unique(clubs,'club',errors);unique(players,'player',errors,true);unique(competitions,'competition',errors);const sid=new Set(sectors.map(x=>x.nickname)),cid=new Set(clubs.map(x=>x.nickname));clubs.forEach(x=>(x.sectors||[x.sector]).filter(Boolean).forEach(s=>{if(!sid.has(s))errors.push(`club ${x.nickname}: unknown sector ${s}`)}));players.forEach(x=>{(x.clubs||[x.club]).filter(Boolean).forEach(c=>{if(!cid.has(c))errors.push(`player ${x.nickname}: unknown club ${c}`)});(x.sectors||[x.sector]).filter(Boolean).forEach(s=>{if(!sid.has(s))errors.push(`player ${x.nickname}: unknown sector ${s}`)})});competitions.forEach(x=>{if(x.sector&&!sid.has(x.sector))errors.push(`competition ${x.nickname}: unknown sector ${x.sector}`);if(x.sport&&!['football','futsal','cricsal'].includes(x.sport))errors.push(`competition ${x.nickname}: invalid sport ${x.sport}`)});const s=d.dashboard?.sports;if(s&&(s.football?.goals<0||s.futsal?.goals<0||s.cricsal?.runs<0||s.cricsal?.wickets<0))errors.push('dashboard: negative sport statistic');if(d.dashboard?.mock)warnings.push('Dashboard data is marked mock');return{ok:!errors.length,errors,warnings}}async function load(){if(cache)return cache;const entries=await Promise.all(Object.entries(files).map(async([k,p])=>[k,await fetchJSON(p)]));const d=Object.fromEntries(entries),v=validate(d);cache={...d,...v,sectors:arr(d.sectors,'sectors'),clubs:arr(d.clubs,'clubs'),players:arr(d.players,'players'),competitions:arr(d.competitions,'competitions')};return cache}return{load,validate,clear:()=>cache=null}})();
