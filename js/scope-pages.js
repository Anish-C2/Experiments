(async()=>{try{
 const type=document.body.dataset.page;if(type!=='matches'&&type!=='records')return;
 const d=await CASPER_DATA.load();const q=new URLSearchParams(location.search);const requested=q.get('sector')||'ALL';const scope=d.scope(requested)||d.scope('ALL');const host=document.querySelector('#directory');if(!host)return;
 const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
 const stat=(l,v,n='')=>`<div><small>${esc(l)}</small><strong>${esc(v)}</strong><span>${esc(n)}</span></div>`;
 const chips=page=>`<div class="chip-row">${[{nickname:'ALL'},...d.sectors].map(s=>{const id=s.nickname||'ALL';return `<a class="chip${id===scope.id?' active':''}" href="${page}${id!=='ALL'?'?sector='+encodeURIComponent(id):''}">${esc(id==='ALL'?'ALL SECTORS':id)}</a>`}).join('')}</div>`;
 const s=scope.sports;
 const nameOf=(kind,id)=>{const row=d.get&&d.get(kind,id);return (row&&(row.name||row.nickname))||id||'—';};
 const csnAwardBoard=(model,sectorId)=>{
  const rows=(model.csnAwards||[]).filter(a=>!sectorId||sectorId==='ALL'||a.sector===sectorId);
  if(!rows.length) return `<section class="box" style="margin-top:10px"><header><div><small>CSN aw()</small><h2>NO AWARDS IN SCOPE</h2></div></header><p class="empty-state">No aw() blocks in the loaded CSN files.</p></section>`;
  return `<section class="box" style="margin-top:10px"><header><div><small>CSN aw()</small><h2>${rows.length} OFFICIAL AWARDS</h2></div><span>FROM SEASON FILES</span></header>${rows.map(a=>`<div class="list-row"><b>${esc(String(a.sport||'').toUpperCase())}</b><strong>${esc(a.award)}</strong><span>${esc(a.name||a.competition||'')} · ${esc(a.sector||'')}</span><small>${esc(nameOf(model.get&&model.get('player',a.winner)?'player':'club', a.winner))}</small></div>`).join('')}</section>`;
 };
 if(type==='matches'){
  const icons={football:'\u26bd',futsal:'\ud83e\udde6',cricsal:'\ud83c\udfcf'};
  host.innerHTML=`${chips('matches.html')}<div class="stats">${stat('TOTAL MATCHES',scope.results.length,scope.label)}${stat('FOOTBALL',s.football.matches,'matches')}${stat('FOOTBALL GOALS',s.football.goals,'goals')}${stat('FUTSAL',s.futsal.matches,'matches')}${stat('FUTSAL GOALS',s.futsal.goals,'goals')}${stat('CRICSAL',s.cricsal.matches,'matches')}${stat('CRICSAL RUNS',s.cricsal.runs,'runs')}${stat('CRICSAL WICKETS',s.cricsal.wickets,'wickets')}</div><section class="box" style="margin-top:10px"><header><div><small>RESULT ARCHIVE</small><h2>MATCH CENTRE</h2></div><span id="filter-status">ALL SPORTS</span></header><div class="chip-row" id="sport-switch"><button class="chip active" data-s="all">ALL</button><button class="chip" data-s="football">FOOTBALL</button><button class="chip" data-s="futsal">FUTSAL</button><button class="chip" data-s="cricsal">CRICSAL</button></div><div id="match-list"></div></section>`;
  const draw=list=>{host.querySelector('#match-list').innerHTML=list.map(r=>{const home=d.get('club',r.home),away=d.get('club',r.away);return `<div class="result"><small>${esc(r.status)}<br>${esc(r.competition)} · ${esc(r.round)}</small><strong>${esc(home?.name||r.home)}<br>${esc(away?.name||r.away)}</strong><b>${esc(r.score)}</b><small>${esc(r.duration||'')}</small><em>${icons[r.sport]||''}</em></div>`}).join('')||'<div class="empty-state">No matches recorded for this filter.</div>';};
  draw(scope.results);host.querySelectorAll('#sport-switch button').forEach(b=>b.onclick=()=>{host.querySelectorAll('#sport-switch button').forEach(x=>x.classList.toggle('active',x===b));const sport=b.dataset.s;host.querySelector('#filter-status').textContent=sport==='all'?'ALL SPORTS':sport.toUpperCase();draw(sport==='all'?scope.results:scope.results.filter(r=>r.sport===sport));});
 }
 if(type==='records'){
  const recordRows=[];for(const [sport,x] of Object.entries(s)){const total=sport==='cricsal'?x.runs:x.goals;recordRows.push({label:`${sport.toUpperCase()} TOTAL`,value:total,note:`${x.matches} matches`});recordRows.push({label:`${sport.toUpperCase()} AVERAGE`,value:x.average,note:sport==='cricsal'?'runs / match':'goals / match'});if(sport==='cricsal')recordRows.push({label:'CRICSAL WICKETS',value:x.wickets,note:`${x.matches} matches`});}
  host.innerHTML=`${chips('records.html')}<div class="stats">${stat('DATASET',d.mock?'MOCK DATA':'CSN ARCHIVE',scope.label)}${stat('FOOTBALL MATCHES',s.football.matches,'football only')}${stat('FOOTBALL GOALS',s.football.goals,'football only')}${stat('FUTSAL MATCHES',s.futsal.matches,'futsal only')}${stat('FUTSAL GOALS',s.futsal.goals,'futsal only')}${stat('CRICSAL MATCHES',s.cricsal.matches,'cricsal only')}${stat('CRICSAL RUNS',s.cricsal.runs,'cricsal only')}${stat('CRICSAL WICKETS',s.cricsal.wickets,'cricsal only')}</div><section class="box" style="margin-top:10px"><header><div><small>DERIVED RECORD BOARD</small><h2>${esc(scope.label)}</h2></div><span>CSN SOURCE</span></header><div class="record-list">${recordRows.map(r=>`<div><small>${esc(r.label)}</small><b>${esc(r.value)}</b><span>${esc(r.note)}</span></div>`).join('')}</div></section><div class="detail-grid"><div class="box"><header><div><small>FOOTBALL</small><h2>GOALS</h2></div></header><div class="record-list">${stat('TOTAL GOALS',s.football.goals,scope.label)}${stat('MATCHES',s.football.matches,scope.label)}${stat('AVERAGE',s.football.average,'goals / match')}</div></div><div class="box"><header><div><small>FUTSAL</small><h2>GOALS</h2></div></header><div class="record-list">${stat('TOTAL GOALS',s.futsal.goals,scope.label)}${stat('MATCHES',s.futsal.matches,scope.label)}${stat('AVERAGE',s.futsal.average,'goals / match')}</div></div><div class="box"><header><div><small>CRICSAL</small><h2>RUNS + WICKETS</h2></div></header><div class="record-list">${stat('TOTAL RUNS',s.cricsal.runs,scope.label)}${stat('WICKETS',s.cricsal.wickets,scope.label)}${stat('MATCHES',s.cricsal.matches,scope.label)}${stat('AVERAGE',s.cricsal.average,'runs / match')}</div></div></div>` + csnAwardBoard(d, scope.id);
 }
}catch(e){console.error(e);document.querySelector('main')?.insertAdjacentHTML('afterbegin',`<div class="data-error">CASPER DATA ERROR · ${String(e.message||e)}</div>`);}})();
