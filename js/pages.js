const CIcon={football:'⚽',futsal:'🥅',cricsal:'🏏'};
const escPage=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const pageSport=s=>s==='cricsal'?'CRICSAL':String(s||'').toUpperCase();
const keyPage=v=>String(v??'').trim().toLowerCase();
const byPage=(a,k,v)=>a.find(x=>keyPage(x?.[k])===keyPage(v));
const stat=(label,value,note='')=>`<div><small>${escPage(label)}</small><strong>${escPage(value)}</strong><span>${escPage(note)}</span></div>`;
const primaryClub=p=>Array.isArray(p?.clubs)?p.clubs[0]:(p?.club||'');
const playerClubs=p=>Array.isArray(p?.clubs)?p.clubs:(p?.club?[p.club]:[]);
const playerSectors=p=>Array.isArray(p?.sectors)?p.sectors:(p?.sector?[p.sector]:[]);
const sportBlock=(sport,x)=>{if(!x)return '';const primary=sport==='cricsal'?(x.runs??0):(x.goals??0);return `<div class="box"><header><div><small>${CIcon[sport]||''} ${pageSport(sport)}</small><h2>STATISTICS</h2></div><span>${x.matches??0} MATCHES</span></header><div class="record-list">${stat(sport==='cricsal'?'RUNS':'GOALS',primary,'recorded total')}${stat(sport==='cricsal'?'WICKETS':'MATCHES',sport==='cricsal'?(x.wickets??0):(x.matches??0),'recorded')}${stat('AVERAGE',x.average??'—',sport==='cricsal'?'runs per match':'goals per match')}</div></div>`};
const entityStats=(sport,rows)=>{const z={matches:0,goals:0,runs:0,wickets:0};for(const r of rows||[]){if(sport&&r.sport!==sport)continue;z.matches++;if(r.sport==='cricsal'){z.runs+=Number(r.runs??r.homeScore??0)+Number(r.awayScore??0)-((r.homeScore??0)+(r.awayScore??0)-(r.runs??0));z.wickets+=Number(r.wickets||0);}else z.goals+=Number(r.homeScore||0)+Number(r.awayScore||0);}const total=sport==='cricsal'?z.runs:z.goals;z.average=z.matches?(total/z.matches).toFixed(2):'—';return z;};
const matchRows=(d,filter)=>{const rows=d.network?.results||[];return filter?rows.filter(filter):rows;};
const linkedResults=(d,c)=>matchRows(d,r=>keyPage(r.competition)===keyPage(c.nickname));
const clubResults=(d,id)=>matchRows(d,r=>keyPage(r.home)===keyPage(id)||keyPage(r.away)===keyPage(id));
const playerStats=(d,id)=> (d.network?.playerStats||[]).filter(x=>keyPage(x.player)===keyPage(id));
const renderStatStrip=(items)=>`<div class="stats">${items.join('')}</div>`;
const empty=msg=>`<div class="empty-state">${escPage(msg)}</div>`;

(async()=>{try{
 const d=await CASPER_DATA.load();
 const q=new URLSearchParams(location.search);const id=q.get('id');const type=document.body.dataset.page;const $=s=>document.querySelector(s);
 document.querySelectorAll('.topbar nav a').forEach(a=>{if(a.href.includes(type))a.classList.add('active')});
 const setHead=(title,sub)=>{document.title=`CASPER — ${title}`;if($('#page-title'))$('#page-title').textContent=title;if($('#page-sub'))$('#page-sub').textContent=sub};
 const sectorStats=s=>d.bySector?.[s.nickname]||{sports:{football:{},futsal:{},cricsal:{}},clubs:0,players:0,competitions:0,results:[]};
 const sportStats=(rows,s)=>entityStats(s,rows);

 if(type==='sectors'){
  const host=$('#directory');if(!host)throw new Error('Missing #directory');
  const n=d.network;host.innerHTML=renderStatStrip([
   stat('SECTORS',n.sectors,'network'),stat('ACTIVE',n.active,'operating'),stat('CLUBS',n.clubs,'network'),stat('PLAYERS',n.players,'network'),stat('COMPETITIONS',n.competitions,'network'),stat('FOOTBALL GOALS',n.sports.football.goals,'football'),stat('FUTSAL GOALS',n.sports.futsal.goals,'futsal'),stat('CRICSAL RUNS',n.sports.cricsal.runs,'cricsal')
  ])+`<div class="sector-grid">${d.sectors.map((s,i)=>{const x=sectorStats(s);return `<a class="directory-card" href="sector.html?id=${encodeURIComponent(s.nickname)}"><div class="directory-kicker">SECTOR ${String(i+1).padStart(2,'0')} · ${escPage(s.nickname)}</div><h2>${escPage(s.name)}</h2><p>${escPage(s.description||'')}</p><div class="directory-meta"><b>${x.clubs}<small>CLUBS</small></b><b>${x.players}<small>PLAYERS</small></b><b>${x.competitions}<small>COMPETITIONS</small></b><b>${escPage(s.status).toUpperCase()}<small>STATUS</small></b></div><div class="sport-split">${['football','futsal','cricsal'].map(k=>{const z=x.sports[k]||{};return `<div><b>${CIcon[k]} ${pageSport(k)}</b><strong>${k==='cricsal'?(z.runs??0):(z.goals??0)}</strong><span>${k==='cricsal'?'RUNS':'GOALS'} · ${z.matches??0} MATCHES</span></div>`}).join('')}</div></a>`}).join('')||empty('No sectors registered.')}</div>`;
 }
 if(type==='clubs'){
  let rows=d.clubs.slice();const host=$('#directory');if(!host)throw new Error('Missing #directory');
  const render=()=>{const table=d.network.clubTable||[];host.innerHTML=renderStatStrip([stat('CLUBS',d.clubs.length,'registered'),stat('PLAYERS',d.players.length,'network'),stat('COMPETITIONS',d.competitions.length,'network'),stat('FOOTBALL MATCHES',d.network.sports.football.matches,'network'),stat('FOOTBALL GOALS',d.network.sports.football.goals,'football'),stat('FUTSAL GOALS',d.network.sports.futsal.goals,'futsal'),stat('CRICSAL RUNS',d.network.sports.cricsal.runs,'cricsal'),stat('CRICSAL WICKETS',d.network.sports.cricsal.wickets,'cricsal')])+`<div class="directory">${rows.map(c=>{const t=byPage(table,'club',c.nickname);return `<a class="list-row" href="club.html?id=${encodeURIComponent(c.nickname)}"><b>${escPage(c.nickname)}</b><strong>${escPage(c.name)}</strong><span>${escPage(c.sector)} · ${t?`P ${t.p} · W ${t.w} · D ${t.d} · L ${t.l} · GF ${t.gf} · GA ${t.ga}`:'no football table row'}</span><small>${t?`ELO ${t.elo} · ${t.wr}`:'VIEW →'}</small></a>`}).join('')||empty('No clubs registered.')}</div>`};render();$('#filter')?.addEventListener('input',e=>{const v=keyPage(e.target.value);rows=d.clubs.filter(c=>keyPage(`${c.name} ${c.nickname} ${c.sector}`).includes(v));render()});
 }
 if(type==='players'){
  let rows=d.players.slice();const host=$('#directory');if(!host)throw new Error('Missing #directory');
  const render=()=>{host.innerHTML=renderStatStrip([stat('PLAYERS',d.players.length,'registered'),stat('FOOTBALL GOALS',d.network.sports.football.goals,'football'),stat('FUTSAL GOALS',d.network.sports.futsal.goals,'futsal'),stat('CRICSAL RUNS',d.network.sports.cricsal.runs,'cricsal'),stat('CRICSAL WICKETS',d.network.sports.cricsal.wickets,'cricsal'),stat('FOOTBALL MATCHES',d.network.sports.football.matches,'football'),stat('FUTSAL MATCHES',d.network.sports.futsal.matches,'futsal'),stat('CRICSAL MATCHES',d.network.sports.cricsal.matches,'cricsal')])+`<div class="directory">${rows.map(p=>{const ps=playerStats(d,p.nickname);const clubs=playerClubs(p).join(', ');const sectors=playerSectors(p).join(', ');const totalGoals=ps.filter(x=>x.sport!=='cricsal').reduce((a,x)=>a+Number(x.primary||0),0);const runs=ps.filter(x=>x.sport==='cricsal').reduce((a,x)=>a+Number(x.primary||0),0);return `<a class="list-row" href="player.html?id=${encodeURIComponent(p.nickname)}"><b>${escPage(p.nickname)}</b><strong>${escPage(p.name)}</strong><span>${escPage(clubs||'—')} · ${escPage(sectors.join(', ')||'—')}</span><small>${totalGoals} GOALS · ${runs} RUNS · ${ps.reduce((a,x)=>a+Number(x.apps||0),0)} APP</small></a>`}).join('')||empty('No players registered.')}</div>`};render();$('#filter')?.addEventListener('input',e=>{const v=keyPage(e.target.value);rows=d.players.filter(p=>keyPage(`${p.name} ${p.nickname} ${playerClubs(p).join(' ')} ${playerSectors(p).join(' ')}`).includes(v));render()});
 }
 if(type==='competitions'){
  let rows=d.competitions.slice();const host=$('#directory');if(!host)throw new Error('Missing #directory');
  const render=()=>{host.innerHTML=renderStatStrip([stat('COMPETITIONS',rows.length,'registry'),stat('FOOTBALL',d.competitions.filter(c=>c.sport==='football').length,'competitions'),stat('FUTSAL',d.competitions.filter(c=>c.sport==='futsal').length,'competitions'),stat('CRICSAL',d.competitions.filter(c=>c.sport==='cricsal').length,'competitions'),stat('ACTIVE',d.competitions.filter(c=>c.status==='active').length,'status'),stat('LIVE',d.competitions.filter(c=>c.status==='live').length,'status'),stat('SECTORS',new Set(d.competitions.map(c=>c.sector)).size,'represented'),stat('SEASONS',new Set(d.competitions.map(c=>c.season)).size,'represented')])+`<div class="directory">${rows.map(c=>{const r=linkedResults(d,c);const s=sportStats(r,c.sport);const total=c.sport==='cricsal'?s.runs:s.goals;return `<a class="list-row" href="competition.html?id=${encodeURIComponent(c.nickname)}"><b>${CIcon[c.sport]||''} ${escPage(c.nickname)}</b><strong>${escPage(c.name)}</strong><span>${escPage(c.sector)} · ${pageSport(c.sport)} · ${escPage(c.type||'')}</span><small>${r.length?`${total} ${c.sport==='cricsal'?'RUNS':'GOALS'} · ${r.length} MATCHES`:'NO MATCH DATA'}</small></a>`}).join('')||empty('No competitions registered.')}</div>`};render();$('#filter')?.addEventListener('input',e=>{const v=keyPage(e.target.value);rows=d.competitions.filter(c=>keyPage(`${c.name} ${c.nickname} ${c.sector} ${c.sport} ${c.type}`).includes(v));render()});
 }
 if(type==='sector'){
  const s=byPage(d.sectors,'nickname',id)||d.sectors[0];const x=sectorStats(s);const sp=x.sports||{};setHead(s.name,`${s.region||s.nickname} · ${s.description||''}`);$('#directory').innerHTML=renderStatStrip([stat('CLUBS',x.clubs,'registered'),stat('PLAYERS',x.players,'registered'),stat('COMPETITIONS',x.competitions,'registered'),stat('MATCHES',(d.bySector?.[s.nickname]?.results||[]).length,'archive'),stat('FOOTBALL GOALS',sp.football?.goals||0,'football only'),stat('FUTSAL GOALS',sp.futsal?.goals||0,'futsal only'),stat('CRICSAL RUNS',sp.cricsal?.runs||0,'cricsal only'),stat('CRICSAL WICKETS',sp.cricsal?.wickets||0,'cricsal only')])+`<div class="detail-grid">${sportBlock('football',sp.football)}${sportBlock('futsal',sp.futsal)}${sportBlock('cricsal',sp.cricsal)}<div class="box"><header><div><small>REGISTRY</small><h2>ENTITIES</h2></div><span>${x.clubs+x.players} ITEMS</span></header>${d.clubs.filter(c=>keyPage(c.sector)===keyPage(s.nickname)).map(c=>`<a class="mini-link" href="club.html?id=${encodeURIComponent(c.nickname)}"><b>${escPage(c.nickname)}</b>${escPage(c.name)}</a>`).join('')}${d.players.filter(p=>playerSectors(p).some(sx=>keyPage(sx)===keyPage(s.nickname))).map(p=>`<a class="mini-link" href="player.html?id=${encodeURIComponent(p.nickname)}"><b>${escPage(p.nickname)}</b>${escPage(p.name)}</a>`).join('')||empty('No entities registered in this sector.')}</div></div>`;
 }
 if(type==='club'){
  const c=byPage(d.clubs,'nickname',id)||d.clubs[0];const t=byPage(d.network.clubTable,'club',c.nickname);const players=d.players.filter(p=>playerClubs(p).some(x=>keyPage(x)===keyPage(c.nickname)));const results=clubResults(d,c.nickname);const f=entityStats('football',results),fu=entityStats('futsal',results),cr=entityStats('cricsal',results);setHead(c.name,`${c.nickname} · ${c.sector||''} · registered club`);$('#directory').innerHTML=renderStatStrip([stat('SECTOR',c.sector||'—','primary'),stat('SQUAD',players.length,'registered'),stat('FOOTBALL P',t?.p??0,'table'),stat('WINS',t?.w??0,'football'),stat('DRAWS',t?.d??0,'football'),stat('LOSSES',t?.l??0,'football'),stat('GF',t?.gf??0,'football'),stat('GA',t?.ga??0,'football')])+`<div class="detail-grid">${sportBlock('football',f)}${sportBlock('futsal',fu)}${sportBlock('cricsal',cr)}<div class="box"><header><div><small>SQUAD REGISTRY</small><h2>${players.length} PLAYERS</h2></div></header>${players.map(p=>{const ps=playerStats(d,p.nickname);const total=ps.filter(x=>x.sport!=='cricsal').reduce((a,x)=>a+Number(x.primary||0),0);const runs=ps.filter(x=>x.sport==='cricsal').reduce((a,x)=>a+Number(x.primary||0),0);return `<a class="mini-link" href="player.html?id=${encodeURIComponent(p.nickname)}"><b>${escPage(p.nickname)}</b>${escPage(p.name)} · ${total} GOALS · ${runs} RUNS</a>`}).join('')||empty('No registered players.')}</div></div>`;
 }
 if(type==='player'){
  const p=byPage(d.players,'nickname',id)||d.players[0];const c=byPage(d.clubs,'nickname',primaryClub(p));const rows=playerStats(d,p.nickname);setHead(p.name,`${p.nickname} · ${playerSectors(p).join(', ')||'—'} · player registry`);const goals=rows.filter(x=>x.sport!=='cricsal').reduce((a,x)=>a+Number(x.primary||0),0),runs=rows.filter(x=>x.sport==='cricsal').reduce((a,x)=>a+Number(x.primary||0),0),apps=rows.reduce((a,x)=>a+Number(x.apps||0),0);const cards=rows.length?rows.map(x=>`<div class="box"><header><div><small>${CIcon[x.sport]||''} ${pageSport(x.sport)}</small><h2>PERFORMANCE</h2></div><span>${x.apps||0} APPS</span></header><div class="record-list">${stat(x.sport==='cricsal'?'RUNS':'GOALS',x.primary||0,'primary')}${stat('SECONDARY',x.secondary||0,x.sport==='cricsal'?'wickets / secondary':'assists / secondary')}${stat('APPEARANCES',x.apps||0,'recorded')}${stat('DISCIPLINE',x.discipline||0,'recorded')}</div></div>`).join(''):`<div class="box"><header><div><small>SPORT STATISTICS</small><h2>NO ARCHIVE TOTALS</h2></div></header><p>Statistics will appear when official match records are linked.</p></div>`;$('#directory').innerHTML=renderStatStrip([stat('GENERAL ID',p.nickname,'identity'),stat('BIRTH YEAR',p.birthyear||'—','registry'),stat('CLUBS',playerClubs(p).length,'registered'),stat('SECTORS',playerSectors(p).length,'represented'),stat('SPORT ROWS',rows.length,'derived'),stat('GOALS',goals,'football + futsal'),stat('CRICSAL RUNS',runs,'cricsal'),stat('APPEARANCES',apps,'recorded')])+`<div class="detail-grid">${cards}<div class="box"><header><div><small>REGISTRY</small><h2>PROFILE</h2></div></header><div class="record-list">${stat('NAME',p.name,'registered')}${stat('CURRENT CLUB',c?.name||primaryClub(p)||'—','registry')}${stat('SECTORS',playerSectors(p).join(', ')||'—','registry')}${stat('REGISTERED',p.date_registered||'—','date')}</div></div></div>`;
 }
 if(type==='competition'){
  const c=byPage(d.competitions,'nickname',id)||d.competitions[0];const rows=linkedResults(d,c);const s=entityStats(c.sport,rows);const participants=new Set(rows.flatMap(r=>[r.home,r.away]));setHead(c.name,`${c.nickname} · ${c.sector||'—'} · ${pageSport(c.sport)} · ${c.status||'registered'}`);$('#directory').innerHTML=`<div class="competition-shell">${renderStatStrip([stat('SPORT',pageSport(c.sport),'competition'),stat('MATCHES',rows.length,'recorded'),stat(c.sport==='cricsal'?'RUNS':'GOALS',c.sport==='cricsal'?s.runs:s.goals,'competition total'),stat('AVERAGE',s.average,c.sport==='cricsal'?'runs / match':'goals / match'),stat('PARTICIPANTS',participants.size,'clubs')])}<div class="detail-grid"><div class="box"><header><div><small>COMPETITION PROFILE</small><h2>METADATA</h2></div></header><div class="record-list">${stat('GENERAL ID',c.nickname,'identity')}${stat('SECTOR',c.sector||'—','registry')}${stat('SEASON',c.season||'—','registry')}${stat('TYPE',c.type||'—','format')}${stat('STATUS',c.status||'—','state')}</div></div><div class="box"><header><div><small>PARTICIPANTS</small><h2>${participants.size}</h2></div></header>${[...participants].map(id=>{const x=byPage(d.clubs,'nickname',id);return `<a class="mini-link" href="club.html?id=${encodeURIComponent(id)}"><b>${escPage(id)}</b>${escPage(x?.name||id)}</a>`}).join('')||empty('No participants recorded.')}</div><div class="box"><header><div><small>RESULTS</small><h2>MATCH CENTRE</h2></div><span>${rows.length} MATCHES</span></header>${rows.map(r=>{const h=byPage(d.clubs,'nickname',r.home),a=byPage(d.clubs,'nickname',r.away);return `<div class="result"><small>${escPage(r.round)}<br>${escPage(r.status)}</small><strong>${escPage(h?.name||r.home)}<br>${escPage(a?.name||r.away)}</strong><b>${escPage(r.score)}</b><small>${escPage(r.duration||'')}</small><em>${CIcon[r.sport]||''}</em></div>`}).join('')||empty('No recorded matches.')}</div></div></div>`;
 }
}catch(e){console.error(e);document.querySelector('main')?.insertAdjacentHTML('afterbegin',`<div class="data-error">CASPER DATA ERROR · ${escPage(e.message)}</div>`);}})();
