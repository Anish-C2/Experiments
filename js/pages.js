const CIcon={football:'⚽',futsal:'🥅',cricsal:'🏏'};
const escPage=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const pageSport=s=>s==='cricsal'?'CRICSAL':String(s||'').toUpperCase();
const by=(a,k,v)=>a.find(x=>x[k]===v);
const norm=v=>String(v||'').toLowerCase().replace(/[^a-z0-9]/g,'');
const stat=(label,value,note='')=>`<div><small>${escPage(label)}</small><strong>${escPage(value)}</strong><span>${escPage(note)}</span></div>`;
const sportBlock=(sport,x)=>{if(!x)return '';return `<div class="box"><header><div><small>${CIcon[sport]||''} ${pageSport(sport)}</small><h2>STATISTICS</h2></div></header><div class="record-list">${stat(sport==='cricsal'?'RUNS':'GOALS',x.goals??x.runs??0,'recorded total')}${stat(sport==='cricsal'?'WICKETS':'MATCHES',x.wickets??x.matches??0,'recorded matches')}${stat('AVERAGE',x.average??'—',sport==='cricsal'?'runs per match':'goals per match')}</div></div>`};
(async()=>{try{const d=await CASPER_DATA.load();const q=new URLSearchParams(location.search);const id=q.get('id');const type=document.body.dataset.page;const $=s=>document.querySelector(s);document.querySelectorAll('.topbar nav a').forEach(a=>{if(a.href.includes(type))a.classList.add('active')});
const allResults=d.dashboard?.results||d.results||[];
const linkedResults=c=>allResults.filter(r=>norm(r.competition)===norm(c.nickname));
const scoreParts=r=>String(r.score||'').split(/[–-]/).map(Number);
const entityStats=(sport,rows)=>{const z={matches:0,goals:0,runs:0,wickets:0};rows.filter(r=>!sport||r.sport===sport).forEach(r=>{const n=scoreParts(r);if(n.length===2&&n.every(Number.isFinite)){z.matches++;if(r.sport==='cricsal')z.runs+=n[0]+n[1];else z.goals+=n[0]+n[1];}});if(sport==='cricsal')return {...z,average:z.matches?(z.runs/z.matches).toFixed(2):'—'};return {...z,average:z.matches?(z.goals/z.matches).toFixed(2):'—'};};
if(type==='sectors'){const host=$('#directory');const network=d.dashboard.network||{};const sTotals=d.dashboard.sectorStats||{};host.innerHTML=`<div class="stats">${stat('SECTORS',network.sectors??d.sectors.length,'network')}${stat('ACTIVE',network.active??d.sectors.filter(x=>x.status==='active').length,'operating')}${stat('CLUBS',network.clubs??d.clubs.length,'network')}${stat('PLAYERS',network.players??d.players.length,'network')}${stat('COMPETITIONS',network.competitions??d.competitions.length,'network')}${stat('FOOTBALL GOALS',d.dashboard.sports?.football?.goals??0,'goals')}${stat('FUTSAL GOALS',d.dashboard.sports?.futsal?.goals??0,'goals')}${stat('CRICSAL RUNS',d.dashboard.sports?.cricsal?.runs??0,'runs')}</div><div class="sector-grid">${d.sectors.map((s,i)=>{const x=sTotals[s.nickname]||{},sp=x.sports||{};return `<a class="directory-card" href="sector.html?id=${encodeURIComponent(s.nickname)}"><div class="directory-kicker">SECTOR ${String(i+1).padStart(2,'0')} · ${escPage(s.nickname)}</div><h2>${escPage(s.name)}</h2><p>${escPage(s.description)}</p><div class="directory-meta"><b>${x.clubs??d.clubs.filter(c=>c.sector===s.nickname).length}<small>CLUBS</small></b><b>${x.players??d.players.filter(p=>p.sector===s.nickname).length}<small>PLAYERS</small></b><b>${x.competitions??d.competitions.filter(c=>c.sector===s.nickname).length}<small>COMPETITIONS</small></b><b>${escPage(s.status).toUpperCase()}<small>STATUS</small></b></div><div class="sport-split">${['football','futsal','cricsal'].map(k=>{const a=sp[k]||{};return `<div><b>${CIcon[k]} ${pageSport(k)}</b><strong>${k==='cricsal'?(a.runs??0):(a.goals??0)}</strong><span>${k==='cricsal'?'RUNS':'GOALS'} · ${a.matches??0} MATCHES</span></div>`}).join('')}</div></a>`}).join('')}</div>`;}
if(type==='clubs'){const rows=d.clubs;const table=d.dashboard.clubTable||[];const render=()=>{$('#directory').innerHTML=`<div class="stats">${stat('CLUBS',rows.length,'registered')}${stat('PLAYERS',d.players.length,'network registry')}${stat('COMPETITIONS',d.competitions.length,'registry')}${stat('FOOTBALL MATCHES',d.dashboard.sports?.football?.matches??0,'network')}${stat('FOOTBALL GOALS',d.dashboard.sports?.football?.goals??0,'network')}${stat('FUTSAL GOALS',d.dashboard.sports?.futsal?.goals??0,'separate')}${stat('CRICSAL RUNS',d.dashboard.sports?.cricsal?.runs??0,'separate')}${stat('CRICSAL WICKETS',d.dashboard.sports?.cricsal?.wickets??0,'separate')}</div><div class="directory">${rows.map(c=>{const t=by(table,'club',c.nickname);return `<a class="list-row" href="club.html?id=${encodeURIComponent(c.nickname)}"><b>${escPage(c.nickname)}</b><strong>${escPage(c.name)}</strong><span>${escPage(c.sector)} · ${t?`P ${t.p} · W ${t.w} · D ${t.d} · L ${t.l} · GF ${t.gf} · GA ${t.ga}`:'no table row'}</span><small>${t?`ELO ${t.elo} · ${t.wr}`:'VIEW →'}</small></a>`}).join('')}</div>`};render();$('#filter')?.addEventListener('input',e=>{const v=e.target.value.toLowerCase();$('#directory').innerHTML=d.clubs.filter(c=>(c.name+c.nickname+c.sector).toLowerCase().includes(v)).map(c=>`<a class="list-row" href="club.html?id=${encodeURIComponent(c.nickname)}"><b>${escPage(c.nickname)}</b><strong>${escPage(c.name)}</strong><span>${escPage(c.sector)}</span><small>VIEW →</small></a>`).join('')});}
if(type==='players'){let rows=d.players;const render=()=>{$('#directory').innerHTML=`<div class="stats">${stat('PLAYERS',d.players.length,'registered')}${stat('FOOTBALL GOALS',d.dashboard.sports?.football?.goals??0,'network')}${stat('FUTSAL GOALS',d.dashboard.sports?.futsal?.goals??0,'network')}${stat('CRICSAL RUNS',d.dashboard.sports?.cricsal?.runs??0,'network')}${stat('CRICSAL WICKETS',d.dashboard.sports?.cricsal?.wickets??0,'network')}${stat('FOOTBALL MATCHES',d.dashboard.sports?.football?.matches??0,'network')}${stat('FUTSAL MATCHES',d.dashboard.sports?.futsal?.matches??0,'network')}${stat('CRICSAL MATCHES',d.dashboard.sports?.cricsal?.matches??0,'network')}</div><div class="directory">${rows.map(p=>{const x=by(d.dashboard.playerStats||[],'player',p.nickname);return `<a class="list-row" href="player.html?id=${encodeURIComponent(p.nickname)}"><b>${escPage(p.nickname)}</b><strong>${escPage(p.name)}</strong><span>${escPage(p.club)} · ${escPage(p.sector)}${x?' · '+pageSport(x.sport):''}</span><small>${x?`${x.primary} ${x.sport==='cricsal'?'RUNS':'GOALS'} · ${x.apps} APP`:'VIEW →'}</small></a>`}).join('')}</div>`};render();$('#filter')?.addEventListener('input',e=>{const v=e.target.value.toLowerCase();rows=d.players.filter(p=>(p.name+p.nickname+p.club+p.sector).toLowerCase().includes(v));render()});}
if(type==='competitions'){let rows=d.competitions;const render=()=>{$('#directory').innerHTML=`<div class="stats">${stat('COMPETITIONS',rows.length,'registry')}${stat('FOOTBALL',d.competitions.filter(c=>c.sport==='football').length,'competitions')}${stat('FUTSAL',d.competitions.filter(c=>c.sport==='futsal').length,'competitions')}${stat('CRICSAL',d.competitions.filter(c=>c.sport==='cricsal').length,'competitions')}${stat('ACTIVE',d.competitions.filter(c=>c.status==='active').length,'status')}${stat('LIVE',d.competitions.filter(c=>c.status==='live').length,'status')}${stat('SECTORS',new Set(d.competitions.map(c=>c.sector)).size,'represented')}${stat('SEASONS',new Set(d.competitions.map(c=>c.season)).size,'represented')}</div><div class="directory">${rows.map(c=>{const r=linkedResults(c);const s=entityStats(c.sport,r);return `<a class="list-row" href="competition.html?id=${encodeURIComponent(c.nickname)}"><b>${CIcon[c.sport]||''} ${escPage(c.nickname)}</b><strong>${escPage(c.name)}</strong><span>${escPage(c.sector)} · ${pageSport(c.sport)} · ${escPage(c.type)}</span><small>${r.length?`${c.sport==='cricsal'?s.runs:s.goals} ${c.sport==='cricsal'?'RUNS':'GOALS'} · ${r.length} MATCHES`:'NO MATCH DATA'}</small></a>`}).join('')}</div>`};render();$('#filter')?.addEventListener('input',e=>{const v=e.target.value.toLowerCase();rows=d.competitions.filter(c=>(c.name+c.nickname+c.sector+c.sport+c.type).toLowerCase().includes(v));render()});}
if(type==='sector'){const s=by(d.sectors,'nickname',id)||d.sectors[0];const x=(d.dashboard.sectorStats||{})[s.nickname]||{};const sp=x.sports||{};document.title=`CASPER — ${s.name}`;$('#page-title').textContent=s.name;$('#page-sub').textContent=`${s.region||s.nickname} · ${s.description}`;$('#directory').innerHTML=`<div class="stats">${stat('CLUBS',x.clubs??d.clubs.filter(c=>c.sector===s.nickname).length,'registered')}${stat('PLAYERS',x.players??d.players.filter(p=>p.sector===s.nickname).length,'registered')}${stat('COMPETITIONS',x.competitions??d.competitions.filter(c=>c.sector===s.nickname).length,'registered')}${stat('FORM',x.form??'—','recent')}${stat('FOOTBALL GOALS',sp.football?.goals??0,'football only')}${stat('FUTSAL GOALS',sp.futsal?.goals??0,'futsal only')}${stat('CRICSAL RUNS',sp.cricsal?.runs??0,'cricsal only')}${stat('CRICSAL WICKETS',sp.cricsal?.wickets??0,'cricsal only')}</div><div class="detail-grid">${sportBlock('football',sp.football)}${sportBlock('futsal',sp.futsal)}${sportBlock('cricsal',sp.cricsal)}<div class="box"><header><div><small>REGISTRY</small><h2>ENTITIES</h2></div></header>${d.clubs.filter(c=>c.sector===s.nickname).map(c=>`<a class="mini-link" href="club.html?id=${c.nickname}"><b>${escPage(c.nickname)}</b>${escPage(c.name)}</a>`).join('')}${d.players.filter(p=>p.sector===s.nickname).map(p=>`<a class="mini-link" href="player.html?id=${p.nickname}"><b>${escPage(p.nickname)}</b>${escPage(p.name)}</a>`).join('')}</div></div>`;}
if(type==='club'){const c=by(d.clubs,'nickname',id)||d.clubs[0];const t=by(d.dashboard.clubTable||[],'club',c.nickname);const players=d.players.filter(p=>p.club===c.nickname);const pstats=(d.dashboard.playerStats||[]).filter(p=>p.club===c.nickname);const results=allResults.filter(r=>r.home===c.nickname||r.away===c.nickname);const football=entityStats('football',results),futsal=entityStats('futsal',results),cricsal=entityStats('cricsal',results);document.title=`CASPER — ${c.name}`;$('#page-title').textContent=c.name;$('#page-sub').textContent=`${c.nickname} · ${c.sector} · registered club`;$('#directory').innerHTML=`<div class="stats">${stat('SQUAD',players.length,'registered players')}${stat('FOOTBALL P',t?.p??'—','table')}${stat('WINS',t?.w??'—','football table')}${stat('DRAWS',t?.d??'—','football table')}${stat('LOSSES',t?.l??'—','football table')}${stat('GF',t?.gf??'—','football')}${stat('GA',t?.ga??'—','football')}${stat('ELO',t?.elo??'—','rating')}</div><div class="detail-grid">${sportBlock('football',t?{goals:t.gf,matches:t.p,average:t.p?(t.gf/t.p).toFixed(2):'—'}:football)}${sportBlock('futsal',futsal)}${sportBlock('cricsal',cricsal)}<div class="box"><header><div><small>SQUAD REGISTRY</small><h2>${players.length} PLAYERS</h2></div></header>${players.map(p=>{const z=pstats.find(x=>x.player===p.nickname);return `<a class="mini-link" href="player.html?id=${p.nickname}"><b>${escPage(p.nickname)}</b>${escPage(p.name)}${z?` · ${z.primary} ${z.sport==='cricsal'?'RUNS':'GOALS'}`:''}</a>`}).join('')||'<p>No registered players.</p>'}</div></div>`;}
if(type==='player'){const p=by(d.players,'nickname',id)||d.players[0];const c=by(d.clubs,'nickname',p.club);const rows=(d.dashboard.playerStats||[]).filter(x=>x.player===p.nickname);document.title=`CASPER — ${p.name}`;$('#page-title').textContent=p.name;$('#page-sub').textContent=`${p.nickname} · ${p.sector} · player registry`;const cards=rows.length?rows.map(x=>`<div class="box"><header><div><small>${CIcon[x.sport]||''} ${pageSport(x.sport)}</small><h2>PERFORMANCE</h2></div></header><div class="record-list">${stat(x.sport==='cricsal'?'RUNS':'GOALS',x.primary,'primary stat')}${stat('SECONDARY',x.secondary,'secondary stat')}${stat('APPEARANCES',x.apps,'apps')}${stat('DISCIPLINE',x.discipline,'recorded')}</div></div>`).join(''):'<div class="box"><header><div><small>SPORT STATISTICS</small><h2>NO ARCHIVE TOTALS</h2></div></header><p>Statistics will appear when official match records are linked.</p></div>';$('#directory').innerHTML=`<div class="stats">${stat('GENERAL ID',p.nickname,'identity')}${stat('BIRTH YEAR',p.birthyear||'—','registry')}${stat('CLUB',p.club||'—','current')}${stat('SECTOR',p.sector||'—','current')}${stat('SPORTS',rows.length,'stat rows')}${stat('GOALS',rows.filter(x=>x.sport!=='cricsal').reduce((a,x)=>a+x.primary,0),'football + futsal, separated below')}${stat('CRICSAL RUNS',rows.filter(x=>x.sport==='cricsal').reduce((a,x)=>a+x.primary,0),'cricsal only')}${stat('APPEARANCES',rows.reduce((a,x)=>a+x.apps,0),'recorded')}</div><div class="detail-grid">${cards}<div class="box"><header><div><small>CURRENT CLUB</small><h2>${escPage(c?.name||p.club||'—')}</h2></div></header><a class="mini-link" href="club.html?id=${encodeURIComponent(p.club)}"><b>${escPage(p.club)}</b>Open club profile →</a><p>Registered: <b>${escPage(p.date_registered||'—')}</b></p></div></div>`;}
if(type==='competition'){const c=by(d.competitions,'nickname',id)||d.competitions[0];const results=linkedResults(c);const clubs=[...new Set(results.flatMap(r=>[r.home,r.away]))].map(x=>by(d.clubs,'nickname',x)).filter(Boolean);const s=entityStats(c.sport,results);document.title=`CASPER — ${c.name}`;$('#page-title').textContent=c.name;$('#page-sub').textContent=`${c.nickname} · ${c.sector} · ${pageSport(c.sport)} · ${c.season}`;const primary=c.sport==='cricsal'?s.runs:s.goals;$('#directory').innerHTML=`<div class="competition-shell"><section class="comp-stats"><div class="comp-stat"><small>MATCHES</small><strong>${results.length}</strong><span>linked result rows</span></div><div class="comp-stat"><small>PARTICIPANTS</small><strong>${clubs.length}</strong><span>clubs in linked results</span></div><div class="comp-stat"><small>${c.sport==='cricsal'?'RUNS':'GOALS'}</small><strong>${primary}</strong><span>sport-specific total</span></div><div class="comp-stat"><small>${c.sport==='cricsal'?'WICKETS':'AVG GOALS / MATCH'}</small><strong>${c.sport==='cricsal'?'—':s.average}</strong><span>${c.sport==='cricsal'?'scorecard data':'derived'}</span></div><div class="comp-stat"><small>FORMAT</small><strong>${escPage(c.type).toUpperCase()}</strong><span>${escPage(c.ruleset)} ruleset</span></div></section><section class="comp-main"><div class="box comp-results"><header><div><small>MATCH CENTRE</small><h2>RESULTS</h2></div><span>${results.length} LINKED</span></header>${results.map(r=>{const h=by(d.clubs,'nickname',r.home),a=by(d.clubs,'nickname',r.away);return `<a class="comp-match" href="matches.html"><span>${escPage(r.status)}</span><div><b>${escPage(h?.name||r.home)}</b><em>${escPage(r.score)}</em><b>${escPage(a?.name||r.away)}</b></div><small>${escPage(r.round)} · ${escPage(r.duration)} · ${pageSport(r.sport)}</small></a>`}).join('')||'<div class="empty-state">No match rows are linked to this competition.</div>'}</div><div class="box comp-side"><header><div><small>COMPETITION FILE</small><h2>PROFILE</h2></div></header><div class="comp-meta"><span>GENERAL ID<b>${escPage(c.nickname)}</b></span><span>SECTOR<b><a href="sector.html?id=${encodeURIComponent(c.sector)}">${escPage(c.sector)}</a></b></span><span>SEASON<b>${escPage(c.season)}</b></span><span>SPORT<b>${CIcon[c.sport]||''} ${pageSport(c.sport)}</b></span><span>TYPE<b>${escPage(c.type)}</b></span><span>STATUS<b>${escPage(c.status)}</b></span><span>RULESET<b>${escPage(c.ruleset)}</b></span></div></div><div class="box comp-participants"><header><div><small>PARTICIPANTS</small><h2>CLUBS</h2></div><span>${clubs.length} FOUND</span></header>${clubs.map(x=>`<a class="mini-link" href="club.html?id=${encodeURIComponent(x.nickname)}"><b>${escPage(x.nickname)}</b>${escPage(x.name)}</a>`).join('')||'<div class="empty-state">No linked participants.</div>'}</div><div class="box comp-stage"><header><div><small>COMPETITION STRUCTURE</small><h2>${escPage(c.type).toUpperCase()}</h2></div></header><div class="stage-line"><b>${escPage(c.season)}</b><span>→</span><b>${escPage(c.ruleset).toUpperCase()}</b><span>→</span><b>${escPage(c.status).toUpperCase()}</b></div><p>${c.type==='cup'?'Knockout competition. Recorded stages will populate the bracket as competition-linked CSN records expand.':'League competition. Competition standings will derive from competition-linked match records.'}</p></div></section></div>`;}
}catch(e){document.querySelector('main')?.insertAdjacentHTML('afterbegin',`<div class="data-error">CASPER DATA ERROR · ${escPage(e.message)}</div>`);console.error(e)}})();