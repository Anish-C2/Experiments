const CIcon={football:'⚽',futsal:'🥅',cricsal:'🏏'};
const escPage=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const pageSport=s=>s==='cricsal'?'CRICSAL':String(s||'').toUpperCase();
const sportPrimary=s=>s==='cricsal'?'RUNS':'GOALS';
const sportSecondary=s=>s==='cricsal'?'WICKETS':'MATCHES';
(async()=>{try{const d=await CASPER_DATA.load();const q=new URLSearchParams(location.search);const id=q.get('id');const type=document.body.dataset.page;const $=s=>document.querySelector(s);const by=(a,k,v)=>a.find(x=>x[k]===v);
const nav=()=>document.querySelectorAll('.topbar nav a').forEach(a=>{if(a.href.includes(type))a.classList.add('active')});nav();
if(type==='sectors'){const host=$('#directory');host.innerHTML=d.sectors.map((s,i)=>{const clubs=d.clubs.filter(c=>c.sector===s.nickname).length,players=d.players.filter(p=>p.sector===s.nickname).length,comps=d.competitions.filter(c=>c.sector===s.nickname).length;return `<a class="directory-card" href="sector.html?id=${encodeURIComponent(s.nickname)}"><div class="directory-kicker">SECTOR ${String(i+1).padStart(2,'0')} · ${escPage(s.nickname)}</div><h2>${escPage(s.name)}</h2><p>${escPage(s.description)}</p><div class="directory-meta"><b>${clubs}<small>CLUBS</small></b><b>${players}<small>PLAYERS</small></b><b>${comps}<small>COMPETITIONS</small></b><b>${escPage(s.status).toUpperCase()}<small>STATUS</small></b></div></a>`}).join('');}
if(type==='clubs'){let rows=d.clubs;const render=()=>{$('#directory').innerHTML=rows.map(c=>`<a class="list-row" href="club.html?id=${encodeURIComponent(c.nickname)}"><b>${escPage(c.nickname)}</b><strong>${escPage(c.name)}</strong><span>${escPage(c.sector)}</span><small>VIEW →</small></a>`).join('')};render();$('#filter')?.addEventListener('input',e=>{const v=e.target.value.toLowerCase();rows=d.clubs.filter(c=>(c.name+c.nickname+c.sector).toLowerCase().includes(v));render()});}
if(type==='players'){let rows=d.players;const render=()=>{$('#directory').innerHTML=rows.map(p=>`<a class="list-row" href="player.html?id=${encodeURIComponent(p.nickname)}"><b>${escPage(p.nickname)}</b><strong>${escPage(p.name)}</strong><span>${escPage(p.club)} · ${escPage(p.sector)}</span><small>${p.birthyear||'—'} · VIEW →</small></a>`).join('')};render();$('#filter')?.addEventListener('input',e=>{const v=e.target.value.toLowerCase();rows=d.players.filter(p=>(p.name+p.nickname+p.club+p.sector).toLowerCase().includes(v));render()});}
if(type==='competitions'){let rows=d.competitions;const render=()=>{$('#directory').innerHTML=rows.map(c=>`<a class="list-row" href="competition.html?id=${encodeURIComponent(c.nickname)}"><b>${CIcon[c.sport]||''} ${escPage(c.nickname)}</b><strong>${escPage(c.name)}</strong><span>${escPage(c.sector)} · ${pageSport(c.sport)} · ${escPage(c.type)}</span><small>${escPage(c.status).toUpperCase()} · VIEW →</small></a>`).join('')};render();$('#filter')?.addEventListener('input',e=>{const v=e.target.value.toLowerCase();rows=d.competitions.filter(c=>(c.name+c.nickname+c.sector+c.sport+c.type).toLowerCase().includes(v));render()});}
if(type==='sector'){const s=by(d.sectors,'nickname',id)||d.sectors[0];document.title=`CASPER — ${s.name}`;$('#page-title').textContent=s.name;$('#page-sub').textContent=`${s.region} · ${s.description}`;const clubs=d.clubs.filter(c=>c.sector===s.nickname),players=d.players.filter(p=>p.sector===s.nickname),comps=d.competitions.filter(c=>c.sector===s.nickname);$('#directory').innerHTML=`<div class="detail-grid"><div class="box"><header><div><small>REGISTERED CLUBS</small><h2>${clubs.length}</h2></div></header>${clubs.map(c=>`<a class="mini-link" href="club.html?id=${c.nickname}"><b>${escPage(c.nickname)}</b>${escPage(c.name)}</a>`).join('')}</div><div class="box"><header><div><small>REGISTERED PLAYERS</small><h2>${players.length}</h2></div></header>${players.map(p=>`<a class="mini-link" href="player.html?id=${p.nickname}"><b>${escPage(p.nickname)}</b>${escPage(p.name)}</a>`).join('')}</div><div class="box"><header><div><small>COMPETITIONS</small><h2>${comps.length}</h2></div></header>${comps.map(c=>`<a class="mini-link" href="competition.html?id=${c.nickname}"><b>${CIcon[c.sport]} ${escPage(c.nickname)}</b>${escPage(c.name)}</a>`).join('')}</div></div>`;}
if(type==='club'){const c=by(d.clubs,'nickname',id)||d.clubs[0];document.title=`CASPER — ${c.name}`;$('#page-title').textContent=c.name;$('#page-sub').textContent=`${c.nickname} · ${c.sector} · registered club`;const players=d.players.filter(p=>p.club===c.nickname);$('#directory').innerHTML=`<div class="detail-grid"><div class="box"><header><div><small>GENERAL ID</small><h2>${escPage(c.nickname)}</h2></div></header><p>Sector: <b>${escPage(c.sector)}</b></p></div><div class="box"><header><div><small>SQUAD REGISTRY</small><h2>${players.length}</h2></div></header>${players.map(p=>`<a class="mini-link" href="player.html?id=${p.nickname}"><b>${escPage(p.nickname)}</b>${escPage(p.name)}</a>`).join('')||'<p>No registered players.</p>'}</div><div class="box"><header><div><small>STATISTICS</small><h2>SPORT SPLITS</h2></div></header><p>Club profile statistics are derived from match records and will populate from the archive.</p></div></div>`;}
if(type==='player'){const p=by(d.players,'nickname',id)||d.players[0];const c=by(d.clubs,'nickname',p.club);document.title=`CASPER — ${p.name}`;$('#page-title').textContent=p.name;$('#page-sub').textContent=`${p.nickname} · ${p.sector} · player registry`;$('#directory').innerHTML=`<div class="detail-grid"><div class="box"><header><div><small>GENERAL ID</small><h2>${escPage(p.nickname)}</h2></div></header><p>Birth year: <b>${p.birthyear||'—'}</b></p><p>Registered: <b>${escPage(p.date_registered||'—')}</b></p></div><div class="box"><header><div><small>CURRENT CLUB</small><h2>${escPage(c?.name||p.club||'—')}</h2></div></header><a class="mini-link" href="club.html?id=${escPage(p.club)}"><b>${escPage(p.club)}</b>Open club profile →</a></div><div class="box"><header><div><small>SPORT STATISTICS</small><h2>ARCHIVE</h2></div></header><p>Statistics are calculated from the underlying CSN archive; no profile-level totals are treated as authoritative.</p></div></div>`;}
if(type==='competition'){const c=by(d.competitions,'nickname',id)||d.competitions[0];document.title=`CASPER — ${c.name}`;$('#page-title').textContent=c.name;$('#page-sub').textContent=`${c.nickname} · ${c.sector} · ${pageSport(c.sport)} · ${c.season}`;const results=(d.results||[]).filter(r=>r.competition===c.nickname);const clubs=[...new Set(results.flatMap(r=>[r.home,r.away]))].map(x=>by(d.clubs,'nickname',x)).filter(Boolean);const sport=c.sport;let primary=0,secondary=0;results.forEach(r=>{const nums=String(r.score||'').split('–').map(Number);if(nums.length===2&&nums.every(Number.isFinite)){primary+=nums[0]+nums[1];if(sport==='cricsal')secondary+=0;}});const resultRows=results.map(r=>{const h=by(d.clubs,'nickname',r.home),a=by(d.clubs,'nickname',r.away);return `<a class="comp-match" href="matches.html"><span>${escPage(r.status)}</span><div><b>${escPage(h?.name||r.home)}</b><em>${escPage(r.score)}</em><b>${escPage(a?.name||r.away)}</b></div><small>${escPage(r.round)} · ${escPage(r.duration)} · ${pageSport(r.sport)}</small></a>`}).join('');const participantRows=clubs.map(x=>`<a class="mini-link" href="club.html?id=${encodeURIComponent(x.nickname)}"><b>${escPage(x.nickname)}</b>${escPage(x.name)}</a>`).join('');const sportMetric=sport==='cricsal'?`<div class="comp-stat"><small>RECORDED RUNS</small><strong>${primary}</strong><span>from linked scorecards</span></div><div class="comp-stat"><small>WICKETS</small><strong>—</strong><span>not present in dashboard score rows</span></div>`:`<div class="comp-stat"><small>TOTAL GOALS</small><strong>${primary}</strong><span>across linked matches</span></div><div class="comp-stat"><small>AVG GOALS / MATCH</small><strong>${results.length?(primary/results.length).toFixed(2):'—'}</strong><span>${results.length} linked match${results.length===1?'':'es'}</span></div>`;$('#directory').innerHTML=`<div class="competition-shell"><section class="comp-stats"><div class="comp-stat"><small>MATCHES</small><strong>${results.length}</strong><span>linked archive results</span></div><div class="comp-stat"><small>PARTICIPANTS</small><strong>${clubs.length}</strong><span>clubs in linked results</span></div>${sportMetric}<div class="comp-stat"><small>FORMAT</small><strong>${escPage(c.type).toUpperCase()}</strong><span>${escPage(c.ruleset)} ruleset</span></div></section><section class="comp-main"><div class="box comp-results"><header><div><small>MATCH CENTRE</small><h2>RESULTS</h2></div><span>${results.length} LINKED</span></header>${resultRows||'<div class="empty-state">No match rows are linked to this competition in the current archive dataset.</div>'}</div><div class="box comp-side"><header><div><small>COMPETITION FILE</small><h2>PROFILE</h2></div></header><div class="comp-meta"><span>GENERAL ID<b>${escPage(c.nickname)}</b></span><span>SECTOR<b><a href="sector.html?id=${encodeURIComponent(c.sector)}">${escPage(c.sector)}</a></b></span><span>SEASON<b>${escPage(c.season)}</b></span><span>SPORT<b>${CIcon[c.sport]||''} ${pageSport(c.sport)}</b></span><span>TYPE<b>${escPage(c.type)}</b></span><span>STATUS<b>${escPage(c.status)}</b></span><span>RULESET<b>${escPage(c.ruleset)}</b></span></div></div><div class="box comp-participants"><header><div><small>PARTICIPANTS</small><h2>CLUBS</h2></div><span>${clubs.length} FOUND</span></header>${participantRows||'<div class="empty-state">Participants will appear once competition match records are linked.</div>'}</div><div class="box comp-stage"><header><div><small>COMPETITION STRUCTURE</small><h2>${escPage(c.type).toUpperCase()}</h2></div></header><div class="stage-line"><b>2026A</b><span>→</span><b>${escPage(c.ruleset).toUpperCase()}</b><span>→</span><b>${escPage(c.status).toUpperCase()}</b></div><p>${c.type==='cup'?'Knockout competition. Results are displayed by recorded stage; a full bracket will populate as competition-specific CSN records are linked.':'League competition. Competition-specific standings will populate from linked match records.'}</p></div></section></div>`;}
}catch(e){document.querySelector('main')?.insertAdjacentHTML('afterbegin',`<div class="data-error">CASPER DATA ERROR · ${escPage(e.message)}</div>`);console.error(e)}})();